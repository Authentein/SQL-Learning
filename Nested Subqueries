### Nested Subqueries 

- These are subqueries nested inside other subqueries.
- Can be correlated or uncorrelated.

## DataCamp Example 1: Getting started with nested subqueries

# Instructions:

Complete a nested subquery to select the country_id for 'England Premier League' from league, which will be used to filter the parent subquery.

# Question:
```
SELECT 
    season,
    MAX(home_goal + away_goal) AS max_goals,
    (SELECT MAX(home_goal + away_goal) 
     FROM match 
     WHERE season = main.season
     -- Subquery to get the max goals in an 'England Premier League' match for the same season
     AND country_id IN (____)
    ) AS pl_max_goals
FROM match AS main
GROUP BY season;
```

# Attempt 1:
```
SELECT 
    season,
    MAX(home_goal + away_goal) AS max_goals,
    (SELECT MAX(home_goal + away_goal) 
     FROM match 
     WHERE season = main.season
     -- Subquery to get the max goals in an 'England Premier League' match for the same season
     AND country_id IN (league)
    ) AS pl_max_goals

FROM match AS main
GROUP BY season;
```


# Attempt 2:
```
SELECT 
    season,
    MAX(home_goal + away_goal) AS max_goals,
    (SELECT MAX(home_goal + away_goal) 
     FROM match 
     WHERE season = main.season
     -- Subquery to get the max goals in an 'England Premier League' match for the same season
     AND country_id IN (SELECT country_id FROM league WHERE name = 'England Premier League')
    ) AS pl_max_goals

FROM match AS main
GROUP BY season;
```
- Just completely missed the nested query

# Answer:
```
SELECT 
    season,
    MAX(home_goal + away_goal) AS max_goals,
    (SELECT MAX(home_goal + away_goal) 
     FROM match 
     WHERE season = main.season
     AND country_id IN (SELECT country_id FROM league WHERE name = 'England Premier League')
    ) AS pl_max_goals
FROM match AS main
GROUP BY season;
```

## DataCamp Example 2: Nest a subquery in FROM

- This is going to be a building exercise where the nested query is built through a multiple step process via the various questions

# Instructions:

Generate a list of matches where at least one team scored 5 or more goals.

# Question 1:
```
-- Select matches where a team scored 5+ goals
SELECT
	country_id,
    season,
	id
FROM match
WHERE home_goal ___ OR away_goal ___;
```

# Attempt 1:
```
-- Select matches where a team scored 5+ goals
SELECT
	country_id,
    season,
	id
FROM match
WHERE home_goal >= 5 OR away_goal >= 5;
```

# Answer:
```
SELECT
	country_id,
    season,
	id
FROM match
WHERE home_goal >= 5 OR away_goal >= 5;
```

# Question 2:
```
-- Count match ids
SELECT
    country_id,
    season,
    ___(___) AS matches
-- Set up and alias the subquery
FROM (
	SELECT
    	country_id,
    	season,
    	id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5)
    ___ subquery
GROUP BY country_id, season;
```

# Attempt 1:
```
-- Count match ids
SELECT
    country_id,
    season,
    COUNT(country_id) AS matches
-- Set up and alias the subquery
FROM (
	SELECT
    	country_id,
    	season,
    	id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5)
    AS subquery
GROUP BY country_id, season;
```

# Answer:
```
SELECT
    country_id,
    season,
    COUNT(country_id) AS matches
FROM (
	SELECT
    	country_id,
    	season,
    	id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5)
    AS subquery
GROUP BY country_id, season;
```

- I really need to slow down and read all directions. I got this first try but it took longer than usual because I didn't read the directions.

# Question 3:
```
SELECT
	c.name AS country,
    -- Calculate the average matches per season
	___(___.___) AS avg_seasonal_high_scores
FROM country AS c
-- Left join outer_s to country
___ ___ (
  SELECT country_id, season,
         COUNT(id) AS matches
  FROM (
    SELECT country_id, season, id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5) AS inner_s
  -- Close parentheses and alias the subquery
  GROUP BY country_id, season) ___ outer_s
ON c.id = outer_s.country_id
GROUP BY country;
```

# Attempt 1:
```
SELECT
	c.name AS country,
    -- Calculate the average matches per season
	AVG(outer_s.country_id) AS avg_seasonal_high_scores
FROM country AS c
-- Left join outer_s to country
LEFT JOIN (
  SELECT country_id, season,
         COUNT(id) AS matches
  FROM (
    SELECT country_id, season, id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5) AS inner_s
  -- Close parentheses and alias the subquery
  GROUP BY country_id, season) AS outer_s
ON c.id = outer_s.country_id
GROUP BY country;
```

# Attempt 2:
```
SELECT
	c.name AS country,
    -- Calculate the average matches per season
	AVG(id.match) AS avg_seasonal_high_scores
FROM country AS c
-- Left join outer_s to country
LEFT JOIN (
  SELECT country_id, season,
         COUNT(id) AS matches
  FROM (
    SELECT country_id, season, id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5) AS inner_s
  -- Close parentheses and alias the subquery
  GROUP BY country_id, season) AS outer_s
ON c.id = outer_s.country_id
GROUP BY country;
```

# Attempt 3:
```
SELECT
	c.name AS country,
    -- Calculate the average matches per season
	AVG(outer_s.matches) AS avg_seasonal_high_scores
FROM country AS c
-- Left join outer_s to country
LEFT JOIN (
  SELECT country_id, season,
         COUNT(id) AS matches
  FROM (
    SELECT country_id, season, id
	FROM match
	WHERE home_goal >= 5 OR away_goal >= 5) AS inner_s
  -- Close parentheses and alias the subquery
  GROUP BY country_id, season) AS outer_s
ON c.id = outer_s.country_id
GROUP BY country;
```
- Central isseu here was:
     -- Calculate the average matches per season
	AVG(outer_s.matches) AS avg_seasonal_high_score

- Here is the full breakdown:
    - The Innermost Query (inner_s): This step filters the raw data. It selects only the matches where a high-scoring event occurred (either the home or away team scored 5 or more goals).
    - The Middle Query (outer_s): This step groups those filtered matches by country and by season, using COUNT(id).The output of this subquery is a single number per season.For example, it determines that Country A had 12 high-scoring matches in the 2023 season, 16 in the 2024 season, and 8 in the 2025 season.
    - The Outer Query (AVG(outer_s.matches)): Finally, the main query joins this data back to the country table and groups the final results strictly by country.Because the GROUP BY season flag was already processed inside the subquery, the outer AVG() function looks at the list of seasonal totals (12, 16, 8) and calculates their mathematical average: 12 + 16 + 8 / 3 = 12



## Review:

When you left 28 minutes ago, you worked on Short and Simple Subqueries, chapter 2 of the course Data Manipulation in SQL. Here is what you covered in your last lesson:

You learned about the power and flexibility of subqueries in SQL, specifically how they can be used in the SELECT, FROM, and WHERE clauses to construct complex queries. Subqueries allow you to perform operations that would otherwise require multiple queries or complex joins, making them a vital tool for data analysis and manipulation. Here are the key points:

- Subqueries in SELECT: You discovered how to include a subquery within the SELECT statement to calculate      derived columns, such as the average goals scored across different stages of soccer matches. For instance, using a subquery to determine the overall average goals in a season and comparing it to the stage averages.

- Subqueries in FROM: You learned to use a subquery in the FROM clause to create a derived table, which you can then query as if it were a regular table. This is useful for filtering or aggregating data from the derived table, like finding stages with average home goals higher than the overall average.

- Subqueries in WHERE: You explored how to use subqueries within the WHERE clause to filter records based on a condition that involves a calculation or aggregation, such as selecting stages where the average goals exceed the season's overall average.

- Best Practices: The lesson emphasized the importance of formatting and commenting your SQL queries for readability and maintainability. Proper indentation, use of comments, and alignment of SQL clauses make complex queries with multiple subqueries easier to understand and debug.

Here's an example of using a subquery in the SELECT clause to calculate the overall average goals for the 2012/2013 season and compare it to the average goals per stage:

```
SELECT 
    m.stage,
    ROUND(AVG(m.home_goal + m.away_goal),2) AS avg_goals,
    ROUND((SELECT AVG(home_goal + away_goal) 
           FROM match 
           WHERE season = '2012/2013'),2) AS overall
FROM match AS m
WHERE m.season = '2012/2013'
GROUP BY m.stage;
```

This query demonstrates how subqueries can enrich your data analysis by allowing comparisons and calculations that reflect complex relationships within your data.

The goal of the next lesson is to learn how to use correlated subqueries in SQL for dynamic data analysis and manipulation in relational databases.

